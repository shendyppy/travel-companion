# Where the FastAPI service lives. Defaults to the local uvicorn address.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000

# Absolute URL of the deployed site, used for Open Graph and canonical tags.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
