# ==============================================================================
# Travel Companion API — runtime dependencies
# ==============================================================================
# Python 3.13
#
# Direct dependencies only (things our code actually imports). Transitive ones
# are left to the resolver. The previous version mixed both, and as a result
# carried several packages that were never imported anywhere: httpx,
# beautifulsoup4, soupsieve, tenacity, cachetools, websockets. Those are gone,
# along with google-genai/google-auth/google, which became redundant once
# universal_wrapper.py was replaced by LiteLLM.

# --- Web framework ---
fastapi==0.124.4
uvicorn==0.38.0
pydantic==2.12.5

# --- LLM ---
# One interface for every provider, plus normalised streaming and tool-calling.
# Pinned tightly: LiteLLM releases often and has changed behaviour in minor
# versions.
litellm==1.96.2

# --- Flight APIs ---
amadeus==12.0.0
requests==2.32.5

# --- Knowledge base ---
# Vector store for retrieval. Its default embedding model is ONNX and runs
# in-process, which is the point: generation is bring-your-own-key, so retrieval
# must not need an API key of its own.
chromadb==1.5.9

# --- Validation ---
# Tool argument validation for seeded (client-chosen) tool calls. Arrives
# transitively via chromadb, but this code imports it directly, so it is pinned
# here rather than relying on someone else's dependency staying put.
jsonschema==4.26.0

# --- Sessions ---
redis==5.2.1

# --- Config ---
python-dotenv==1.2.1
